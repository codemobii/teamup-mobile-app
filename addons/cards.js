import React from "react";
import { ScrollView } from "react-native-gesture-handler";
import { Text } from "react-native-ui-lib";

export default function CardsADDON() {
  return (
    <>
      {[1, 2, 3, 4, 5, 6, 6].map((e, i) => {
        <Text>Hello</Text>;
      })}
    </>
  );
}
